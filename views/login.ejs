<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/png" href="/public/images/favicon.png" sizes="128x128">
    <title>Log in</title>
</head>
<body>
    <%- include("../views/partials/website-nav") %>
        <main class="center-div">
        <h1>Welcome back!</h1>
        <p class="center-grey">Fill in your details below to access your dashboard</p>
        <form action="/login" method="post">
            <div class="inputGroup">
                <div class="inputDiv">
                    <label for="username">Username</label>
                    <input type="text" value="anakin_skywalker" required name="username" id="username" placeholder="username">
                </div>
                <div class="inputDiv">
                    <label for="password">Password</label>
                    <input type="password" value="Admin1234!!" required name="password" id="password" placeholder="password">
                    <div style="display: flex; padding-top: 10px;">
                        <input type="checkbox" id="reveal" onclick="showPassword()">
                        <label for="reveal" style="margin: 0px; padding: 0px;">Show password</label>
                    </div>
                </div>
                <button type="submit" class="button btn-green">Log in</button>
                <p>Don't have an account? <a href="/create-account">Create an account.</a></p>
            </div>
        </form>
    </main>
    <script>
        function showPassword() {
          var passwordField = document.getElementById("password");
          if (passwordField.type === "password") {
            passwordField.type = "text";
          } else {
            passwordField.type = "password";
          }
        }
    </script>
</body>
</html>